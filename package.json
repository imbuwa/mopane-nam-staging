{
  "name": "mopane-asset-management-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 4173",
    "build": "astro build",
    "build:staging": "PUBLIC_BASE_PATH=/mopane-nam-staging/ PUBLIC_SITE_URL=https://example.github.io/mopane-nam-staging/ astro build",
    "preview": "astro preview --host 127.0.0.1 --port 4173",
    "preview:prod": "astro preview --host 127.0.0.1 --port 4321",
    "check": "astro check && tsc --noEmit",
    "audit:lighthouse": "CHROME_PATH='/Volumes/MAC 3/codex-tools/ms-playwright/chromium-1223/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing' lighthouse http://127.0.0.1:4321/ --only-categories=accessibility,best-practices,seo,performance --chrome-flags='--headless --no-sandbox'"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^6.4.5"
  }
}
